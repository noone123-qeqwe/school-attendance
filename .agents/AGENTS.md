# Agent Rules

- **Automatic Pushes**: Whenever you successfully implement a feature, fix a bug, or make substantial code updates for the user, you MUST automatically commit the changes with a descriptive message and push them to the repository (e.g. `git push origin main`) without waiting for the user to explicitly ask you to "push".
