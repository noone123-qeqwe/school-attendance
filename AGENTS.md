# Workspace Rules & Preferences

## Autonomous Execution
- Work fully autonomously. Execute tasks end-to-end (investigation, implementation, testing, verification, and git push) without prompting for intermediate approvals or confirmations unless critically blocked.

## Git Workflow
- Whenever code changes are completed and verified, automatically stage, commit with a descriptive message, and push to `origin main`.
